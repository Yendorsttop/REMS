# Local-only values; do not use these as production credentials.
DATABASE_URL=postgresql://rems_local:local_only@postgres:5432/rems
PORT=3001
LOG_LEVEL=info
# OIDC_ISSUER is intentionally unset until a production integration is approved.
